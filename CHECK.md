# 확인 방법

압축을 푼 폴더에서 시작합니다. 순서대로 따라가면 20분 안에 전부 확인됩니다.

---

## 0. 실행 — 반드시 로컬 서버로

```bash
cd distribution
python3 -m http.server 8000
```

Windows PowerShell이면:

```powershell
cd distribution
python -m http.server 8000
```

브라우저에서 **http://localhost:8000/** 를 엽니다.

> **`index.html` 을 더블클릭하면 안 됩니다.** ES 모듈과 `fetch`는 `file://` 에서 차단되어 빈 화면이 나옵니다.
> 화면이 비어 있으면 대부분 이 문제입니다. 주소창이 `file:///...` 로 시작하는지 먼저 보세요.
>
> 서버를 끄려면 터미널에서 `Ctrl+C`. 포트 8000이 이미 쓰이는 중이면 `8001` 등으로 바꾸세요.

---

## 1. 자동 검증 — 페이지 6개, 각 5초

브라우저로 아래 여섯 주소를 엽니다. **각 페이지 최상단의 요약 띠가 초록색이고 "전 항목 PASS"** 면 통과입니다.
빨간 띠가 나오면 그 아래 표에서 어느 줄이 FAIL인지 바로 보입니다.

| 주소 | 무엇을 검사하나 | 기대 결과 |
|---|---|---|
| http://localhost:8000/test/numeric.html | 수치 라이브러리 — CDF 정확도, 밀도 적분, PDF↔CDF 정합, 극단 모수, 샘플러, PRNG | 전 항목 PASS — A, B, C, D, E, F (6개 항목) |
| http://localhost:8000/test/data.html | 데이터 정합성 — 노드·엣지 참조, 고아 노드, LaTeX 파싱 | 전 항목 PASS — 분포 21종, 관계 61종, 정합성 9개 항목 |
| http://localhost:8000/test/discrete.html | 이산분포 — PMF·CDF 정확도, 극단 모수, 샘플러 카이제곱, 관계 시뮬레이션, 근사 수렴, 데이터 정합성 | 전 항목 PASS — 7개 항목 |
| http://localhost:8000/test/sim.html | 연속 시뮬레이션 21종의 KS 검정 | 전 항목 PASS — 정확한 변환, 근사 수렴, 정합성 |
| http://localhost:8000/test/clt.html | CLT 시뮬레이터 수용 기준 | 전 항목 PASS — C0 … C7 (8개 항목) |
| http://localhost:8000/test/font-audit.html | 수식 527개 파싱 + KaTeX 폰트 누락 검사 | 전 항목 PASS — 수식 527개 파싱 성공, 폰트 4종 |

`test/sim.html` 과 `test/clt.html` 은 표본을 수십만 개 생성하므로 **결과가 뜨기까지 5~15초** 걸립니다. 정상입니다.

### 표에서 특히 볼 값

- `numeric.html` **A항목** — 최대 상대오차가 전부 `1e-12` 근처여야 합니다. 기준은 `1e-6` 이므로 여유가 큽니다.
- `sim.html` **1번 표** — KS 거리 D가 전부 `0.0073` 미만이어야 합니다. 실제로는 `0.0015~0.0035` 범위입니다.
- `sim.html` **2번 표** — E23·E24의 KS 거리가 위에서 아래로 **계속 줄어들어야** 합니다(↓ 표시).
- `clt.html` **C4** — t(1) 코시에서 n을 1→100으로 늘려도 사분위범위가 줄지 않아야 합니다.
- `clt.html` **C7** — R을 고정한 채 n만 키울 때 왜도가 단조 감소해야 합니다(n 스윕 애니메이션의 근거).
- `discrete.html` **1번 표** — PMF·CDF 최대 상대오차가 `1e-12` 근처여야 합니다(scipy 대조).
- `discrete.html` **3번 표** — 샘플러 카이제곱 14건이 전부 PASS여야 합니다.
- `discrete.html` **2번 항목** — `k = ±∞` 에서 CDF가 정확히 0과 1을 돌려줘야 합니다(경계 가드).

---

## 2. 눈으로 확인 — 핵심 10가지

### ① 감마 프리셋이 원본 교재 그림과 같은가 ★ 가장 중요

http://localhost:8000/index.html#/node/gamma → 오른쪽 패널 아래 **`교재 그림 (λ=2, r=1·4·8)`** 버튼 클릭.

원본으로 주신 감마 그래프와 나란히 놓고 비교하세요. 세 값이 맞아야 합니다.

| 곡선 | 확인 값 |
|---|---|
| r = 1 | x=0 에서 높이 **2.0**, 단조 감소 |
| r = 4 | 최빈값 **1.5**, 높이 약 **0.45** |
| r = 8 | 최빈값 **3.5**, 높이 약 **0.30** |

축은 x 0–20, y 0–2.0으로 고정됩니다.
**여기가 어긋나면 감마 모수화가 rate가 아니라 scale로 들어간 것**이므로 다른 것보다 먼저 알려 주세요.

### ② 관계도 기본 동작

http://localhost:8000/

- 노드에 마우스를 올리면 → 연결된 관계만 남고 나머지가 흐려집니다
- 노드 클릭 → 오른쪽에 분포 패널, 화살표 클릭 → 관계 패널
- 휠로 확대, 드래그로 이동, 좌하단 `전체 보기`로 리셋
- **Tab 키**로 노드 순회, **Enter**로 선택, **방향키**로 인접 노드 이동
- 실선(변환·특수화)과 점선(극한)이 구분되는지

### ③ 유도 과정은 한 번에 다 펼쳐지지 않아야

아무 화살표나 클릭 → `유도 과정` 은 **1단계만** 보이고 `다음 단계 →` 로 하나씩 열립니다.
학생이 스스로 다음 줄을 예측할 여지를 두려는 의도이므로, 처음부터 전부 펼쳐져 있으면 잘못된 것입니다.

### ④ 시뮬레이션이 이론 곡선과 겹치는가

http://localhost:8000/index.html#/edge/E9 (지수 n개의 합 → 감마)

패널 아래 `시뮬레이션` 에서 파란 히스토그램이 검은 점선(이론 밀도)과 겹쳐야 합니다.
`합할 개수 n` 슬라이더를 1→20으로 밀면 모양이 오른쪽으로 이동하며 대칭에 가까워집니다.
`표본 수`를 100으로 낮추면 울퉁불퉁해지고, 100,000으로 올리면 매끄러워집니다.

시뮬레이션이 붙은 관계 27개

- 연속 21개: E1, E9, E10, E11, E13, E14, E19, E20, E21, E23, E24, E26, E27, E28, E29, E30, E31, E32, E36, E38, E39
- 이산 6개: D1, D4, D5, D7, D9, D22

### ⑤ 극한 관계의 수렴 시연

http://localhost:8000/index.html#/edge/E24 (t → N(0,1))

`수렴 시연` 슬라이더를 1 → 200으로 밀면 파란 곡선이 회색 점선(표준정규) 위로 겹쳐집니다.
아래 `최대 밀도 차이` 수치가 함께 줄어듭니다.

### ⑥ 중심극한정리 시뮬레이터 — 교육 장치 4가지

http://localhost:8000/clt.html

이 화면은 아래 넷이 **전부 동작해야** 목적을 달성합니다. 하나씩 눌러 보세요.

1. **n과 R의 구분** — `n`을 2에 두고 `R`만 100 → 50,000으로 올립니다.
   히스토그램이 **매끄러워질 뿐 모양은 그대로**여야 합니다. 안내 문구도 뜹니다.
   그다음 `n`을 30으로 올리면 그제야 정규 모양이 됩니다.
2. **대수의 법칙과의 구분** — `표준화` 체크를 끄면 ②패널의 축이 n=1 스케일로 고정되어,
   n을 키울 때 분포가 **눈에 띄게 좁아지는** 것이 보입니다(LLN). 켜면 ③패널 모양이 N(0,1)로 고정됩니다(CLT).
3. **왜도별 수렴 속도** — `왜도 비교` 버튼 3개를 같은 n에서 차례로 눌러 보세요.
   U(0,1)은 n=5로도 거의 정규, Gamma(0.5)는 n=30에서도 치우쳐 있습니다.
4. **CLT가 실패하는 경우** — 원천분포를 `t 분포`로 고르고 자유도를 **1**로 내립니다.
   주황색 경고 배너가 뜨고, **n을 1→100으로 올려도 히스토그램 폭이 좁아지지 않습니다.**
   자유도를 3 이상으로 올리면 수렴이 살아납니다.

**애니메이션 두 개는 서로 대비되도록 만들었습니다.** 이름에 무엇이 고정인지 적혀 있습니다.

- **`▶ n 늘리기 (R 고정)`** — 표본크기를 1부터 100까지 18단계로 밟으며 모양이 변하는 과정을 재생합니다.
  재생 중에는 **R 슬라이더와 원천분포가 잠기고** "재생 중 고정" 표시가 뜹니다.
  ③패널에 **n=1 시점의 모양이 분홍색 점선 윤곽으로 남아** 출발점과 얼마나 달라졌는지 보입니다.
  통계줄의 `표본왜도`가 함께 줄어드는 것을 보세요 (지수분포 기준 1.99 → 0.20).
- **`▶ R 쌓기 (n 고정)`** — 표본을 하나씩 누적합니다. 히스토그램이 매끄러워질 뿐 **모양은 그대로**입니다.

둘을 차례로 눌러 보면 n과 R의 차이가 한눈에 들어옵니다. 이게 이 화면의 핵심입니다.

`양봉 혼합`, `이산 균등(주사위)` 원천분포도 눌러 보세요 — 정규와 전혀 닮지 않은 분포에서도, 이산분포에서도 성립합니다.

### ⑦ 이산분포가 전체 관계도에 들어갔는가 ★ 이번에 고친 부분

http://localhost:8000/

첨부해 주신 참조 다이어그램과 나란히 놓고 비교하세요.

- 위쪽 띠에 **이산분포 6종**(음이항·기하·포아송·이항·베르누이·초기하)이 테두리가 진한 노드로 있어야 합니다
- 아래쪽에 **코시·와이블·이중지수** 세 노드가 새로 있어야 합니다 (참조 다이어그램에는 있었으나 빠져 있던 것)
- 참조 다이어그램의 화살표가 전부 있는지: Bern→B(ΣXᵢ), B→Bern(n=1), Geom↔NB, Pois↔B(np=λ / X|Σxᵢ),
  HG→B(N→∞), NB→Pois(λ=r(1−p)/p), B→N(μ=np, σ²=np(1−p)), Pois→N(λ=μ=σ²),
  N(0,1)→Cauchy(Z₁/Z₂), t→Cauchy(n=1), Exp↔Weibull, Exp↔DE
- `확장` 체크를 켜면 참조 다이어그램에 없던 보강 관계 11개가 추가로 나타납니다
- **이산 관계도**(상단 메뉴)는 같은 데이터를 이산 중심으로 다시 그린 것입니다. 두 화면의 관계 설명이 같아야 합니다

### ⑧ 라벨이 화살표에 붙어 있는가 ★ 이번에 고친 부분

관계 조건 문자는 **자기 화살표 바로 옆**에 있어야 어느 관계의 조건인지 알 수 있습니다.

- 라벨 49개 중 서로 겹치는 것이 **하나도 없어야** 합니다 (`확장`을 켠 59개 상태에서도)
- 라벨이 분포 상자를 덮고 있으면 안 됩니다
- 첨자 확률변수는 전부 **대문자** — `∑ᵢ Xᵢ`, `∏ᵢ Xᵢ`, `minᵢ Xᵢ` (소문자 xᵢ가 보이면 알려 주세요)
- 겹침은 코드가 자동으로 풉니다. 원래 자리에서 최대 30px까지만 밀리므로 화살표에서 멀어지지 않습니다

### ⑨ 검색·필터·경로 찾기

- 검색창에 `감마` → `분포 5 · 관계 5`. 감마 노드가 진하게, 이웃이 남고 나머지가 흐려집니다
- `관계` 체크박스에서 변환·특수화를 끄면 → `관계 4` (극한 엣지만)
- `확장` 체크 → `관계 59`(기본은 48). 화면을 가로지르는 긴 관계와, 참조 다이어그램에 없던 보강 관계가 나타납니다
- `경로 찾기`에서 표준균등분포 → F 분포 `찾기` → 경로 단계가 표시되고 관계도에 강조됩니다

### ⑩ 학습 경로

툴바 오른쪽 `학습 경로` 에서 **표본분포 삼총사** 선택 → 파란 카드가 뜨고 `다음 →` 로 9단계 진행.
단계마다 오른쪽 패널이 해당 분포/관계로 바뀌고, 경로 전체가 관계도에 강조됩니다.

---

## 3. 오프라인 동작 확인

수식이 외부 CDN 없이 나오는지 확인합니다. **강의실 네트워크에서 수식만 안 나오는 사고를 막는 검사**입니다.

1. 브라우저에서 `F12` → **Network** 탭 → `Offline` 또는 `No throttling` 드롭다운에서 **Offline** 선택
2. 페이지 새로고침 (또는 이미 뜬 상태에서 다른 노드 클릭)
3. **수식이 그대로 렌더링되면 통과**입니다

더 간단한 방법: Wi-Fi를 끄고 http://localhost:8000/ 을 새로고침해도 됩니다(로컬 서버는 계속 돕니다).

---

## 4. 콘솔 오류 확인

`F12` → **Console** 탭. 아래 페이지를 돌면서 **빨간 오류가 하나도 없어야** 합니다.

```
index.html  →  discrete.html  →  clt.html  →  about.html
```

노란 경고 중 `[validate]` 로 시작하는 것이 있으면 데이터 정합성 문제이므로 알려 주세요.

---

## 5. 참조값 재생성 (선택)

`test/numeric.html` 의 A항목이 쓰는 정답 테이블을 R에서 직접 만들 수 있습니다.
제가 만든 값을 믿지 않고 검증하고 싶을 때 쓰세요.

```r
# distribution 폴더에서
Rscript tools/make_reference.R
```

`jsonlite` 만 필요합니다. `test/reference.json` 이 새로 쓰이고, `test/numeric.html` 을 새로고침하면
그 값과 대조합니다. R이 없으면 `python3 tools/make_reference.py` (scipy 필요)도 같은 결과를 냅니다.

> 감마는 `pgamma(x, shape = r, rate = lambda)` 로 되어 있습니다. **rate 모수화**이므로 `scale =` 로 바꾸면 안 됩니다.

---

## 6. 배포

**[DEPLOY.md](DEPLOY.md) 에 단계별로 정리해 두었습니다.** 요약하면, GitHub에 `distribution`
리포지토리를 만들고 폴더 **안의 내용물**을 올린 뒤,
**Settings → Pages → Source: Deploy from a branch → main / (root)** 로 지정하면 끝입니다.

```
https://databetter25.github.io/distribution/
```

`.nojekyll` 파일이 이미 들어 있어 `vendor/` 폴더가 그대로 서빙됩니다. 빌드 과정이 없으므로 별도 설정은 필요 없습니다.

배포 후에는 위 **2번 눈 확인**을 실제 주소에서 한 번 더 돌려 보세요. 특히 ①번(감마 프리셋)과 3번(오프라인)입니다.

---

## 문제가 생기면

| 증상 | 먼저 볼 곳 |
|---|---|
| 화면이 완전히 비어 있다 | 주소가 `file://` 인지 확인. `python3 -m http.server` 로 열어야 합니다 |
| 수식이 `\frac{...}` 같은 원본 텍스트로 보인다 | `vendor/katex/` 폴더가 통째로 빠졌는지 확인 |
| 검증 페이지 A항목만 실패한다 | `test/reference.json` 이 없는 것. 5번의 R/Python 스크립트를 실행하세요 |
| 곡선 모양이 이상하다 | `test/numeric.html` 부터. 렌더링이 아니라 수치 문제일 가능성이 높습니다 |
| 감마·지수·카이제곱만 이상하다 | 거의 확실히 scale/rate 혼동. `js/distributions/pdf.js` 의 `gamma` 확인 |
| 큰 자유도에서 곡선이 사라진다 | 로그 스케일 계산 누락. `λ^r` 이나 `Γ(r)` 을 직접 계산한 곳을 찾으세요 |
| 라벨이 겹친다 | 자동으로 풀립니다(`relaxLabels`). 그래도 겹치면 `data/relations.json` 의 `bend` 를 조정하세요 |
| 화면에 `null` 이 찍힌다 | 조건부 자식에 `append()` 를 쓴 것. `appendChildren()` 으로 바꾸세요 |

수정한 뒤에는 **반드시 1번의 검증 페이지 6개를 다시 확인**하세요.
- 수치 코드를 건드렸다면 `test/numeric.html`
- 수식(LaTeX)을 추가했다면 `test/font-audit.html` — 리포지토리에는 실제로 쓰이는 폰트 4종만 두었기 때문에,
  새 기호가 다른 폰트를 필요로 하면 이 페이지가 "필요한데 파일 없음"으로 잡아 줍니다.
