# PRD — 확률분포 관계 탐색기 (Distribution Relationship Explorer)

| 항목 | 내용 |
|---|---|
| 문서 버전 | **v0.4** (이산분포 통합 — 참조 다이어그램 전체 반영) |
| 작성일 | 2026-09-04 |
| 제품 유형 | 정적 웹사이트 (멀티파일, GitHub Pages 배포) |
| 리포지토리 | `distribution` → `https://databetter25.github.io/distribution/` |
| 주 사용자 | 대학 통계학 수강생 (수리통계학·확률론 수강 수준) |
| 원본 자료 | 분포 관계 다이어그램, 감마 밀도함수 그래프 (r = 1, 4, 8) |
| 참조 다이어그램 | 2026-09-05 재확인 — **20개 노드(연속 14 + 이산 6)**. v0.3까지는 연속 11개만 옮겨져 있었다 |
| 착수 상태 | **미결 항목 없음** — 부록 A 참조 |

---

## 1. 배경과 목적

### 1.1 배경
수리통계학에서 확률분포들은 고립된 개념이 아니라 **변환(transformation)**, **특수화(specialization)**, **극한(limiting behavior)** 이라는 세 가지 관계로 촘촘히 연결된 하나의 그물망이다. 그러나 대부분의 교재는 분포를 한 장(chapter)에 하나씩 순차적으로 제시하기 때문에, 수강생은 "정규분포를 제곱하면 왜 카이제곱이 되는가", "t분포가 n→∞에서 정규분포로 수렴한다는 것이 무슨 뜻인가" 같은 **연결 구조**를 머릿속에서 조립하지 못한 채 개별 공식만 암기하게 된다.

첨부 다이어그램은 이 그물망을 한 장으로 압축한 자료이지만, 정적 이미지이기 때문에 (a) 각 화살표의 조건과 유도 과정을 확인할 수 없고, (b) 모수를 바꿔가며 관계가 실제로 성립하는지 눈으로 볼 수 없다.

### 1.2 목적
이 다이어그램을 **클릭·조작 가능한 학습 도구**로 전환한다. 구체적으로:

1. 관계도를 탐색하며 분포 간 연결 구조를 **지도처럼** 익힌다.
2. 모수 슬라이더로 밀도함수의 변화를 관찰해 **모수의 의미**를 체득한다.
3. 몬테카를로 시뮬레이션으로 각 관계가 **경험적으로 성립함**을 확인한다.
4. 각 관계의 **수학적 유도 과정**을 필요할 때 펼쳐 본다.
5. **중심극한정리**를 직접 조작하며, 왜 정규분포가 이 그물망의 중심에 있는지 납득한다.

### 1.3 성공 지표 (정성)
- 수강생이 임의의 두 분포를 골랐을 때, 사이트에서 둘을 잇는 경로와 조건을 3분 내에 설명할 수 있다.
- 강의 중 교수자가 프로젝터에 띄워 놓고 슬라이더를 조작하며 설명할 수 있을 만큼 반응이 즉각적이다 (조작 → 렌더링 100ms 이내).
- 별도 설치·로그인·네트워크 없이 URL 하나로 접근 가능하다.

---

## 2. 사용자 및 사용 시나리오

### 2.1 페르소나
| 페르소나 | 배경 | 핵심 니즈 |
|---|---|---|
| **P1. 수강생 (주 사용자)** | 학부 3–4학년, 수리통계학 수강 중. 미적분·기초 확률은 알지만 변환 기법(MGF, 야코비안)은 미숙 | 관계의 "왜"를 단계별로 보고 싶다. 시각적 확인으로 직관을 얻고 싶다 |
| **P2. 교수자·조교** | 강의/실습 진행 | 특정 관계를 즉석에서 시연할 수 있는 링크. 슬라이드 대체 |
| **P3. 복습자** | 시험 직전 | 전체 구조를 한눈에 훑고, 헷갈리는 엣지만 빠르게 확인 |

### 2.2 대표 시나리오
- **S1.** 수강생이 관계도에서 `Gamma(r, λ)` 노드를 클릭 → 우측 패널에 정의·PDF·모멘트가 뜨고, 이 노드에 연결된 5개 엣지가 목록으로 표시된다.
- **S2.** `Gamma → Exp` 엣지를 클릭 → "r = 1" 조건과 함께, Gamma PDF에서 r=1을 대입해 지수분포 PDF가 나오는 유도가 LaTeX으로 표시된다. 동시에 상단 플롯에서 r 슬라이더가 1로 이동하며 곡선이 지수분포 형태로 바뀐다.
- **S3.** `t(n) → N(0,1)` (점선, n→∞) 엣지를 클릭 → n 슬라이더를 1부터 200까지 끌면 t 분포 곡선이 표준정규 곡선(고정 참조선) 위로 겹쳐지는 과정을 본다.
- **S4.** 수강생이 시뮬레이션 탭에서 `χ²(m) ← Σ N(0,1)²` 데모를 실행 → 10,000개 표본의 히스토그램이 이론 PDF와 겹쳐 그려진다.
- **S5.** 복습자가 검색창에 "카이제곱"을 입력 → 관련 노드가 하이라이트되고 나머지는 흐려진다.
- **S6.** 교수자가 CLT 강의 중 시뮬레이터를 띄운다. 원천분포를 `양봉 혼합`으로 두고 n을 1 → 2 → 5 → 30으로 올리며, 정규와 전혀 닮지 않은 분포에서도 표본평균이 정규로 모이는 것을 보여준다. 이어 원천분포를 `t(1)`로 바꿔 **CLT가 실패하는 경우**를 대비시킨다.
- **S7.** 수강생이 "표본을 많이 뽑으면 정규가 된다"고 오해한 상태로 들어온다. n=2에 고정한 채 R만 100 → 50,000으로 올려 보고, 히스토그램이 매끄러워질 뿐 정규가 되지 않는 것을 확인하며 n과 R의 차이를 스스로 교정한다.

---

## 3. 정보 구조: 분포와 관계 명세

> 이 절은 구현 시 `data/distributions.json`, `data/relations.json` 의 **원본 명세**로 사용한다.

### 3.1 분포 노드 (11개)

| ID | 표기 | 한글명 | 모수 | 지지집합 | 비고 |
|---|---|---|---|---|---|
| `lognormal` | LN(μ, σ²) | 로그정규분포 | μ ∈ ℝ, σ² > 0 | (0, ∞) | |
| `normal` | N(μ, σ²) | 정규분포 | μ ∈ ℝ, σ² > 0 | ℝ | 그래프의 중심 허브 |
| `stdnormal` | N(0, 1) | 표준정규분포 | 없음 | ℝ | |
| `beta` | Beta(α, β) | 베타분포 | α, β > 0 | (0, 1) | |
| `gamma` | Gamma(r, λ) | 감마분포 | r > 0, λ > 0 | (0, ∞) | **rate 모수화 확정 → §11.2** |
| `exponential` | Exp(λ) | 지수분포 | λ > 0 | (0, ∞) | |
| `chisq` | χ²(m) | 카이제곱분포 | m > 0 (자유도) | (0, ∞) | |
| `t` | t(n) | t 분포 | n > 0 (자유도) | ℝ | |
| `f` | F(r₁, r₂) | F 분포 | r₁, r₂ > 0 | (0, ∞) | |
| `unif01` | U(0, 1) | 표준균등분포 | 없음 | (0, 1) | |
| `unifab` | U(a, b) | 균등분포 | a < b | (a, b) | |

각 노드가 보유해야 할 필드: `id`, `notation`(LaTeX), `nameKo`, `nameEn`, `params[]`(기호·기본값·범위·제약), `support`, `pdf`(LaTeX + JS 함수), `cdf`, `mean`, `variance`, `mgf`, `description`(2–4문장 한국어), `commonUse`(활용 맥락 1–2문장).

### 3.2 관계 엣지 (25개)

관계 유형은 세 가지로 분류한다.
- **`transform`** (실선): 확률변수 변환. 예 `Y = exp(X)`
- **`special`** (실선): 모수 특수화. 예 `r = 1`
- **`limit`** (점선): 극한/근사. 예 `n → ∞`

| # | From → To | 유형 | 조건/변환 | 정확한 결과 |
|---|---|---|---|---|
| E1 | N(μ,σ²) → LN(μ,σ²) | transform | `Y = exp(X)` | Y ~ LN(μ, σ²) |
| E2 | LN(μ,σ²) → N(μ,σ²) | transform | `Y = ln(X)` | Y ~ N(μ, σ²) |
| E3 | LN → LN | transform | `Y = Πᵢ Xᵢ`, Xᵢ 독립 | Y ~ LN(Σμᵢ, Σσᵢ²) |
| E4 | N → N | transform | `Y = Σᵢ Xᵢ`, Xᵢ 독립 | Y ~ N(Σμᵢ, Σσᵢ²) |
| E5 | N(μ,σ²) → N(0,1) | transform | `Z = (X − μ)/σ` | Z ~ N(0,1) — **원본 표기 오류 §11.1** |
| E6 | N(0,1) → N(μ,σ²) | transform | `Y = μ + σZ` | Y ~ N(μ, σ²) |
| E7 | Gamma(r,λ) → Exp(λ) | special | `r = 1` | Exp(λ) |
| E8 | Gamma(r,λ) → χ²(m) | special | `r = m/2`, `λ = 1/2`(rate) | χ²(m) — **원본 표기 §11.2** |
| E9 | Exp(λ) → Gamma(r,λ) | transform | `Y = Σᵢ₌₁ⁿ Xᵢ`, 독립 | Gamma(n, λ) |
| E10 | Exp(λ) → Exp(·) | transform | `Y = min(X₁,…,Xₙ)` | Exp(nλ) |
| E11 | Gamma → Beta | transform | `Y = X₁/(X₁+X₂)`, 동일 λ, X₁~Γ(α,λ), X₂~Γ(β,λ) | Beta(α, β) |
| E12 | Beta(α,β) → U(0,1) | special | `α = β = 1` | U(0,1) |
| E13 | U(0,1) → Exp(λ) | transform | `Y = −λ⁻¹ ln(X)` | Exp(λ) — 역변환 표본추출 |
| E14 | Exp(λ) → U(0,1) | transform | `Y = exp(−λX)` | U(0,1) |
| E15 | U(0,1) → U(a,b) | transform | `Y = a + (b−a)X` | U(a,b) |
| E16 | U(a,b) → U(0,1) | special | `a = 0, b = 1` | U(0,1) |
| E17 | χ²(m) → Exp(λ) | special | `m = 2` | Exp(1/2) |
| E18 | Exp(λ) → χ²(2) | special | `λ = 1/2` | χ²(2) |
| E19 | χ² → χ² | transform | `Y = Σᵢ Xᵢ`, 독립 | χ²(Σmᵢ) |
| E20 | χ² → F(r₁,r₂) | transform | `Y = (X₁/r₁)/(X₂/r₂)`, 독립 | F(r₁, r₂) |
| E21 | t(n) → F(1,n) | transform | `Y = X²` | F(1, n) |
| E22 | Beta(α,β) → N(μ,σ²) | limit | `α = β → ∞` | 정규 근사 |
| E23 | Gamma(r,λ) → N(μ,σ²) | limit | `r → ∞`, μ=r/λ, σ²=r/λ² | 정규 근사 (CLT) — **§11.2로 라벨 수정** |
| E24 | t(n) → N(0,1) | limit | `n → ∞` | N(0,1) |
| E25 | F(r₁,r₂) → χ²(r₁) | limit | `r₂ → ∞`, `Y = r₁·X` | χ²(r₁) |

각 엣지가 보유해야 할 필드: `id`, `from`, `to`, `type`, `conditionLatex`, `transformLatex`, `summaryKo`(한 줄 요약), `derivation`(단계별 유도 배열: 각 단계는 `{latex, explanationKo}`), `derivationMethod`(`cdf` | `jacobian` | `mgf` | `substitution` | `clt`), `simulation`(시뮬레이션 데모 정의 또는 null).

### 3.3 확장 엣지 (원본 다이어그램에 없음)

원본에 누락되었으나 교육적으로 필요한 관계. **E26·E27은 Phase 1 필수로 확정**하고, 나머지는 Phase 3 후보로 둔다. 원본과 구분되도록 노드/엣지에 `보강` 배지를 붙이고, `about.html`에 "원본 다이어그램에 없던 관계"로 명시한다.

**Phase 1 필수 (확정)**

| # | From → To | 유형 | 변환 | 포함 근거 |
|---|---|---|---|---|
| **E26** | N(0,1) → χ²(1) | transform | `Y = Z²` | χ²의 **정의 그 자체**. 이 엣지가 없으면 그래프에서 정규 계열(N, LN, N(0,1), t)과 표본분포 계열(χ², F)이 감마를 거쳐서만 연결되어, 학생이 `Σ Zᵢ² ~ χ²(n)`이라는 가장 기본적인 사실에 도달하지 못한다 |
| **E27** | N(0,1), χ²(n) → t(n) | transform | `T = Z / √(χ²(n)/n)` | t분포의 **정의**. 원본에는 t(n)으로 **들어오는 엣지가 하나도 없어** t가 고아 노드다. 이 상태로는 "t분포가 어디서 오는가"에 답할 수 없다. E20(F의 정의)과 짝을 이루어 표본분포 삼총사의 구조를 완성한다 |

E26·E27 추가로 F4.6의 시뮬레이션 데모 3번(`Σ N(0,1)² → χ²(m)`)이 원본 엣지에 정식으로 근거를 갖게 되고, F1.8 경로 찾기의 연결성도 크게 개선된다 (예: `U(0,1) → N(0,1) → χ²(1) → F`).

**Phase 3 후보 (보류)**

- `Beta(α,β) → Beta(β,α)` : `Y = 1 − X`
- `F(r₁,r₂) → F(r₂,r₁)` : `Y = 1/X`
- `Beta ↔ F` : `Y = (r₁X)/(r₂(1−X))`
- `모든 연속분포 → U(0,1)` : `Y = F(X)` (확률적분변환) ← E13/E14의 일반형

> 보류 근거: 위 4개는 대칭성·역수 관계로, 알면 유용하지만 **몰라도 다른 관계를 이해하는 데 지장이 없다.** 반면 E26·E27은 없으면 다른 관계를 이해할 수 없는 구조적 링크다. Phase 1에서는 그래프가 복잡해 보이지 않는 것도 중요하므로, 25 + 2 = 27개 엣지로 제한한다.

### 3.4 메타 관계: 중심극한정리 — 엣지가 아닌 별도 화면

**CLT는 관계도의 엣지로 표현하지 않는다.** 이유는 구조적이다.

- CLT는 특정 두 분포의 관계가 아니라 **유한 분산을 갖는 모든 분포에 동시에 적용되는 정리**다. 엣지로 그리면 11개 노드 각각에서 N(0,1)로 향하는 화살표 11개가 생겨, 원본 다이어그램의 가독성이 완전히 무너진다.
- 게다가 CLT는 원본 분포를 **다른 분포로 바꾸는 변환이 아니라**, 표본평균이라는 **새로 정의한 통계량**의 극한 분포에 대한 진술이다. E1–E27의 "확률변수 X를 Y로 바꾼다"와 논리적 층위가 다르다.

따라서 CLT는 **F4.7의 독립 시뮬레이터**로 구현하고, 관계도와는 다음 두 지점에서 연결한다.

| 연결 지점 | 방식 |
|---|---|
| 모든 분포 패널 (F2) | `이 분포로 CLT 확인하기` 버튼 → 해당 분포를 원천으로 시뮬레이터 진입 |
| E22 (Beta α=β→∞), E23 (Gamma r→∞) | 두 엣지는 CLT의 **특수 사례**다. 정수 r에 대해 Gamma(r,λ)는 독립 지수확률변수 r개의 합이므로 E23은 사실상 CLT 그 자체. 관계 패널에 `일반 정리로 보기` 링크 |

> 이 구조 덕분에 학생은 "감마가 정규로 간다"(엣지)와 "**왜** 그런가"(CLT)를 오갈 수 있다. 두 극한 엣지가 고립된 암기 항목이 아니라 하나의 정리의 사례로 읽힌다.

---

## 4. 기능 요구사항

### F1. 인터랙티브 관계도 (핵심)
**우선순위: P0**

- **F1.1** 원본 다이어그램의 노드 배치를 기본 레이아웃으로 재현한다. 강제지향(force-directed) 자동 배치가 아닌 **고정 좌표**를 사용해, 원본 이미지를 본 학생이 동일한 지도로 인식하게 한다. 좌표는 데이터 파일에 명시한다.
- **F1.2** 엣지 유형별 시각 구분:
  - `transform` / `special` → 실선 화살표
  - `limit` → 점선 화살표
  - 범례를 캔버스 우상단에 고정 표시
- **F1.3** 엣지 라벨(조건·변환식)을 화살표 옆에 렌더링한다. 라벨은 LaTeX 수식으로 표시하되, 겹침 방지를 위해 라벨 위치 오프셋을 데이터에 포함시킨다.
- **F1.4** 노드 클릭 → 해당 분포의 상세 패널(F2) 열림. 노드 호버 → 연결된 엣지와 이웃 노드만 강조, 나머지는 opacity 0.2로 감쇠.
- **F1.5** 엣지 클릭 → 관계 상세 패널(F3) 열림.
- **F1.6** 캔버스 확대/축소(휠, 핀치) 및 패닝(드래그). "전체 보기" 리셋 버튼 제공.
- **F1.7** 노드/엣지 선택 상태는 URL 해시에 반영한다 (`#/node/gamma`, `#/edge/E7`). 교수자가 특정 관계로 바로 가는 링크를 공유할 수 있어야 한다.
- **F1.8** 경로 찾기: 두 분포를 선택하면 둘을 잇는 최단 경로를 그래프 위에 강조하고, 경로 상의 변환을 순서대로 나열한다. (예: U(0,1) → Exp(λ) → Gamma(r,λ) → χ²(m))

### F2. 분포 상세 패널 — 모수 슬라이더 + PDF/CDF 플롯
**우선순위: P0**

- **F2.1** 선택된 분포의 정의, LaTeX PDF, 지지집합, 평균, 분산, MGF를 표시한다.
- **F2.2** 모수마다 슬라이더 + 숫자 입력 필드를 제공한다. 슬라이더 범위와 스텝은 분포별로 지정하며, 정의역 제약(σ² > 0 등)을 위반하는 입력은 거부하고 안내 문구를 띄운다.
- **F2.3** PDF 곡선을 실시간 렌더링한다. 슬라이더 조작 중 60fps를 목표로 하되, 최소 100ms 이내 반영.
- **F2.4** PDF / CDF 토글. CDF는 동일 x축 범위를 유지한다.
- **F2.5** x축 범위 자동 조정 (기본: 평균 ± 4σ, 유계 지지집합은 지지집합 전체). 수동 고정 옵션 제공 — 모수를 바꿔도 축이 튀지 않아야 비교가 가능하다.
- **F2.6** **곡선 고정(pin)** 기능: 현재 곡선을 회색으로 고정해 두고 모수를 바꿔 비교한다. 최대 3개까지 고정. — 극한 관계(E22–E25) 학습에 필수.
- **F2.7** 마우스 호버 시 해당 x에서의 밀도값·누적확률을 툴팁으로 표시.
- **F2.8** 해당 노드에 연결된 엣지 목록을 "들어오는 관계 / 나가는 관계"로 나누어 표시. 클릭하면 F3으로 이동.
- **F2.9 교재 그림 프리셋.** 분포마다 교재에서 흔히 쓰는 모수 조합을 버튼으로 제공한다. 클릭하면 해당 곡선들이 F2.6의 고정 곡선으로 한 번에 올라간다.
  - 감마 필수 프리셋 — **`λ = 2` 고정, `r = 1, 4, 8`** (첨부 교재 그림 재현. x축 0–20, y축 0–2.0)
  - 그 외 예시: 정규 `σ = 0.5, 1, 2`, t `n = 1, 5, 30`, 베타 `(0.5,0.5) / (1,1) / (2,5) / (5,5)`
  - 프리셋 정의는 `distributions.json` 의 `presets[]` 필드에 둔다 (라벨·모수조합·축범위 포함)

### F3. 관계 상세 — 유도 과정 보기
**우선순위: P1**

- **F3.1** 엣지의 조건·변환·결과를 상단에 요약 카드로 표시.
- **F3.2** 유도 과정을 **단계별 아코디언**으로 제공한다. 한 번에 모두 펼치지 않고, 단계마다 "다음 단계" 버튼으로 진행 — 학생이 스스로 다음 줄을 예측할 여지를 준다.
- **F3.3** 각 단계는 LaTeX 수식 + 한국어 설명 1–2문장으로 구성한다.
- **F3.4** 유도 기법(`derivationMethod`)을 배지로 표시: `CDF 기법`, `야코비안`, `적률생성함수`, `모수 대입`, `중심극한정리`.
- **F3.5** 극한 관계(`limit`)의 경우 유도 대신 **수렴 시연**을 우선 제공한다: 극한 모수 슬라이더 + 목표 분포 참조 곡선 오버레이 (F2.6 재사용).
- **F3.6** 해당 관계와 연결된 시뮬레이션 데모(F4)로 가는 버튼.

### F4. 시뮬레이션 데모
**우선순위: P1**

- **F4.1** 지정 관계에 대해 몬테카를로 표본을 생성하고, 히스토그램(밀도 정규화)과 이론 PDF를 겹쳐 그린다.
- **F4.2** 표본 크기 선택: 100 / 1,000 / 10,000 / 100,000. "다시 생성" 버튼.
- **F4.3** 재현성을 위해 seed 고정 옵션(seedable PRNG 사용).
- **F4.4** 적합도 요약: 표본평균·표본분산 vs 이론값을 나란히 표시. (형식적 검정은 범위 외 — §9)
- **F4.5** 애니메이션 모드: 표본을 점진적으로 누적하며 히스토그램이 이론 곡선으로 수렴하는 과정을 보여준다. 정지/재생 가능.
- **F4.6** 엣지 연동 데모 (Phase 2, 5종):
  1. `E13` 역변환 표본추출: U(0,1) → Exp(λ)
  2. `E9` 지수 합 → 감마
  3. `E26` Σ N(0,1)² → χ²(m)
  4. `E24` t(n) → N(0,1) 수렴
  5. `E23` Gamma r→∞ 정규 근사

---

### F4.7 중심극한정리 시뮬레이터 (독립 기능)
**우선순위: P0 — Phase 1 포함**

**왜 별도 기능인가.** CLT는 두 분포를 잇는 화살표가 아니다. "유한한 분산을 갖는 **임의의** 분포에서 뽑은 표본평균은 정규로 수렴한다"는 **모든 노드에 동시에 적용되는 메타 관계**이므로, 관계도의 엣지로 그리면 11개 노드에서 N(0,1)로 향하는 화살표 11개가 생겨 그래프가 읽히지 않는다. 대신 **모든 분포 패널에서 진입 가능한 독립 화면**으로 만든다.

E22·E23은 이 메타 관계의 특수 사례다 — 정수 r에 대해 Gamma(r, λ)는 독립 지수확률변수 r개의 합이므로, E23은 사실상 CLT 그 자체다. 두 엣지의 상세 패널에서 이 시뮬레이터로 가는 링크를 제공해 "특수 사례 ↔ 일반 정리"를 연결한다.

#### F4.7.1 화면 구성 — 3단 패널
| 패널 | 내용 |
|---|---|
| **좌: 원천분포** | 선택한 분포의 PDF. 현재 추출된 표본 n개를 x축 위 점(rug)으로 표시. 이론 평균 μ와 표준편차 σ 표기 |
| **중: 표본평균 분포** | 표본평균 `X̄`를 R회 반복해 얻은 히스토그램. 이론 극한 `N(μ, σ²/n)` 곡선 오버레이 |
| **우: 표준화 후** | `Z = (X̄ − μ)/(σ/√n)` 의 히스토그램 + **N(0,1) 참조 곡선 고정** |

우측 패널이 핵심이다. 표준화를 거쳐야 n이 달라져도 같은 축에서 비교할 수 있고, "무엇으로 수렴하는가"가 N(0,1)로 고정된다.

#### F4.7.2 조작 요소
- **원천분포 선택**: 11개 분포 전체 + 교육용 특수 옵션 3종
  - `양봉 혼합` (0.5·N(−2,0.5) + 0.5·N(2,0.5)) — 정규와 전혀 닮지 않은 출발점
  - `극단 왜도` (Gamma r=0.5) — 수렴이 느린 사례
  - `이산 균등 (주사위)` — **이산분포에서도 CLT가 성립함**을 보여주는 유일한 예. §10에서 이산분포를 범위 외로 두었으나 이 데모용으로만 예외 허용
- **표본크기 n 슬라이더**: 1, 2, 3, 5, 10, 30, 50, 100 (이산 스텝). **기본값 1**
- **반복횟수 R 슬라이더**: 100 / 1,000 / 10,000 / 50,000. 기본값 10,000
- **표준화 토글**: 끄면 중앙 패널의 비표준화 `X̄` 를 크게 보여준다 (F4.7.4)
- seed 고정, 다시 생성
- **애니메이션 2종** — 이름에 무엇이 고정인지 드러내어 교육 장치 ①을 조작으로 체험시킨다
  - `▶ n 늘리기 (R 고정)` — n 을 1→100 까지 18단계(1,2,3,4,5,6,8,10,12,15,20,25,30,40,50,65,80,100)로
    밟으며 재생한다. **R 은 사용자가 정한 값에 고정**되고, 재생 중 R 슬라이더·원천분포·모수를 잠가
    "R 고정"을 조작으로도 못 박는다. ③패널에 **n=1 시점의 모양을 점선 윤곽으로 남겨** 출발점과 대비시킨다
  - `▶ R 쌓기 (n 고정)` — 표본을 점진 누적한다. 매끄러워질 뿐 모양이 변하지 않음을 보여 준다

#### F4.7.3 필수 교육 장치 ① — n과 R의 구분
**학생의 가장 흔한 오개념은 "표본을 많이 뽑으면 정규분포가 된다"** 이다. n(한 평균을 만드는 표본 수)과 R(평균을 몇 개 만들었는가)을 혼동하는 데서 온다.

- 두 슬라이더를 **시각적으로 명확히 분리**한다 (다른 색 영역, 각각 아이콘과 한 줄 설명 부착)
  - `n` : "평균 하나를 만드는 데 쓰는 관측값 개수" — **이것이 커져야 정규에 가까워진다**
  - `R` : "평균을 몇 번 반복해서 만들었는가" — **커져도 히스토그램이 매끄러워질 뿐 모양은 안 변한다**
- **직접 확인시키기**: `n=2` 고정 상태에서 R을 100 → 50,000으로 올려 본다. 히스토그램이 매끄러워질 뿐 **여전히 정규가 아니다.** 이때 안내 문구를 띄운다: "반복을 늘려도 모양은 그대로입니다. n을 올려 보세요."
- `n=1` 이 기본값인 이유: 중앙 패널이 원천분포와 **똑같이** 나온다. "평균을 안 냈으니 당연히 원래 분포"라는 출발점을 먼저 눈에 넣어야 이후 변화가 의미를 갖는다.

#### F4.7.4 필수 교육 장치 ② — 대수의 법칙과의 구분
표준화 토글을 끄면 `X̄` 자체의 분포가 보인다. n이 커질수록 **μ 주위로 좁아진다** (LLN). 표준화를 켜면 **N(0,1)로 모양이 고정된다** (CLT).

- 두 상태를 전환하며 관찰하게 하고, 한 줄 설명을 붙인다: "대수의 법칙은 **어디로** 모이는지, 중심극한정리는 **어떤 모양으로** 모이는지를 말합니다."
- 비표준화 상태에서 이론 곡선 `N(μ, σ²/n)` 의 표준편차 `σ/√n` 값을 수치로 표시한다.

#### F4.7.5 필수 교육 장치 ③ — 수렴 속도는 왜도에 달렸다
"n ≥ 30이면 정규"라는 통념을 교정한다.

- 원천분포의 **왜도 γ₁** 를 좌측 패널에 상시 표시한다.
- 정규성 이탈 지표를 우측 패널에 표시한다: 표준화된 표본평균의 표본왜도, 그리고 이론 값 `γ₁/√n`.
- 비교 프리셋 버튼 — 같은 n에서 나란히 확인:

| 원천분포 | 왜도 γ₁ | n=5에서의 인상 |
|---|---|---|
| U(0,1) | 0 | 이미 거의 정규 |
| Exp(λ) | 2 | 여전히 눈에 띄게 오른쪽으로 치우침 |
| Gamma(r=0.5) | ≈2.83 | 확실히 비정규 |

→ 결론 문구: "필요한 n은 분포마다 다릅니다. 대칭이면 n=5로도 충분하고, 왜도가 크면 n=30도 부족합니다."

#### F4.7.6 필수 교육 장치 ④ — CLT가 실패하는 경우
**원천분포로 `t(1)`(코시분포)을 선택할 수 있게 한다.** t(n)은 이미 그래프의 노드이므로 추가 구현 없이 얻어지는 최고의 반례다.

- t(1)은 평균과 분산이 존재하지 않으므로 CLT의 전제가 깨진다. 표본평균은 아무리 n을 키워도 **좁아지지 않고, 여전히 t(1)이다.**
- n 슬라이더를 1 → 100으로 올려도 히스토그램이 변하지 않는 것을 직접 보게 한다.
- 이때 경고 배너를 띄운다: "이 분포는 평균·분산이 존재하지 않아 중심극한정리가 적용되지 않습니다. 표본평균의 분포는 원래 분포와 동일합니다."
- 자유도 슬라이더를 함께 제공해 `t(1) → t(2) → t(3)` 로 옮겨 가며, 분산이 존재하기 시작하는 **n > 2** 부터 수렴이 살아나는 것을 관찰시킨다.
- 히스토그램 축 범위는 극단값 때문에 자동 조정이 무의미하므로, 코시 계열에서는 분위수 기반(1–99%) 축 고정으로 전환한다.

#### F4.7.7 진입 경로
- 전역 내비게이션에 `중심극한정리` 메뉴 상시 노출
- 모든 분포 상세 패널(F2) 하단에 `이 분포로 CLT 확인하기` 버튼 — 클릭 시 해당 분포가 원천분포로 선택된 상태로 진입
- E22·E23 관계 패널(F3)에서 `일반 정리로 보기` 링크
- URL 라우팅: `#/clt?source=exponential&n=5`

#### F4.7.8 성능 요구사항
최악 조건(R=50,000, n=100)은 표본 500만 개 생성이다. 이를 300ms 안에 끝낼 수 없으므로:
- R × n > 1,000,000 이면 **청크 단위 생성 + `requestAnimationFrame` 점진 렌더링**으로 전환하고 진행률을 표시한다 (UI 정지 금지)
- 표본을 배열로 보관하지 않고 히스토그램 빈에 **누적 카운트만** 유지한다 (메모리 상한 고정)
- `Float64Array` 사용, 함수 호출 오버헤드를 줄이기 위해 표본 생성 루프는 분포별로 인라인화

### F5. 검색 및 필터
**우선순위: P2**

- **F5.1** 분포명(한/영), 표기, 별칭으로 검색. 매칭 노드 강조.
- **F5.2** 엣지 유형별 필터 토글 (변환 / 특수화 / 극한). 예를 들어 "극한만 보기"로 근사 관계 구조만 학습.
- **F5.3** 지지집합 필터 (ℝ / (0,∞) / (0,1) / 유계).

### F6. 학습 모드 (가이드 투어)
**우선순위: P2**

- **F6.1** 미리 정의된 학습 경로를 순서대로 안내한다. 각 단계는 특정 노드/엣지로 이동하며 설명 카드를 띄운다.
- **F6.2** Phase 2 제공 경로 (예시):
  - `표본분포 삼총사`: N(0,1) → χ² → t → F
  - `지수족 계보`: U(0,1) → Exp → Gamma → χ²
  - `정규분포로 가는 모든 길`: 극한 엣지 4개 순회
- **F6.3** 진행 상황 표시(3/7 등)와 이전/다음 이동.

---

## 5. 데이터 모델

### 5.1 `data/distributions.json`
```json
{
  "id": "gamma",
  "notation": "\\mathrm{Gamma}(r, \\lambda)",
  "nameKo": "감마분포",
  "nameEn": "Gamma distribution",
  "position": { "x": 420, "y": 300 },
  "params": [
    { "symbol": "r", "nameKo": "형상모수", "default": 2, "min": 0.1, "max": 20, "step": 0.1 },
    { "symbol": "lambda", "notation": "\\lambda", "nameKo": "비율모수", "default": 2, "min": 0.1, "max": 10, "step": 0.1 }
  ],
  "parameterization": "rate",
  "support": { "type": "halfline", "lower": 0, "upper": null, "latex": "(0, \\infty)" },
  "pdfLatex": "f(x) = \\frac{\\lambda^{r}}{\\Gamma(r)} x^{r-1} e^{-\\lambda x}",
  "pdf": "gammaPdf",
  "cdf": "gammaCdf",
  "sampler": "gammaSample",
  "moments": { "meanLatex": "r/\\lambda", "varianceLatex": "r/\\lambda^{2}", "mgfLatex": "(1-t/\\lambda)^{-r},\\ t < \\lambda" },
  "presets": [
    { "labelKo": "교재 그림 (λ=2, r=1·4·8)", "curves": [{"r":1,"lambda":2},{"r":4,"lambda":2},{"r":8,"lambda":2}], "xRange": [0, 20], "yRange": [0, 2.0] }
  ],
  "descriptionKo": "...",
  "commonUseKo": "..."
}
```

### 5.2 `data/relations.json`
```json
{
  "id": "E7",
  "from": "gamma",
  "to": "exponential",
  "type": "special",
  "conditionLatex": "r = 1",
  "transformLatex": null,
  "summaryKo": "감마분포에서 형상모수가 1이면 지수분포가 된다.",
  "labelOffset": { "dx": -20, "dy": 12 },
  "derivationMethod": "substitution",
  "derivation": [
    { "latex": "f(x) = \\frac{1}{\\Gamma(r)\\lambda^{r}} x^{r-1} e^{-x/\\lambda}", "explanationKo": "감마분포의 확률밀도함수에서 출발한다." },
    { "latex": "\\Gamma(1) = 1,\\quad x^{0} = 1", "explanationKo": "r = 1을 대입하면 감마함수 값은 1, x의 거듭제곱 항은 사라진다." },
    { "latex": "f(x) = \\frac{1}{\\lambda} e^{-x/\\lambda}", "explanationKo": "지수분포의 밀도함수와 정확히 일치한다." }
  ],
  "simulation": null
}
```

### 5.3 검증 규칙
빌드 또는 로드 시점에 다음을 자동 검증하고, 실패 시 콘솔에 명시적 오류를 낸다.
- 모든 엣지의 `from`/`to`가 존재하는 노드 ID인가
- 모든 `pdf`/`cdf`/`sampler` 문자열이 실제 등록된 함수명인가
- 모든 LaTeX 문자열이 KaTeX 파싱을 통과하는가
- 각 분포의 PDF가 지지집합 위에서 수치적분 시 1 ± 0.001 인가

---

## 6. 기술 스택 및 파일 구조

### 6.1 스택
| 영역 | 선택 | 근거 |
|---|---|---|
| 빌드 | **없음 (no build step)** | 정적 호스팅. `git push` 만으로 배포. ES modules 직접 사용 |
| 프레임워크 | Vanilla JS (ES2022 modules) | 의존성 최소화, 장기 유지보수 용이 |
| 그래프 렌더링 | **순수 SVG 직접 조작 (D3 미사용)** — 확정 | §6.2 |
| 플롯 | Canvas 2D (곡선), SVG (축·라벨) 혼합 | 슬라이더 실시간 반응 성능 확보 |
| 수식 | **KaTeX, `vendor/` 에 self-host** — 확정 | §6.3 |
| 통계 함수 | **자체 구현** (`js/distributions/`) | 외부 통계 라이브러리 의존 회피. §7 참조 |
| 폰트 | 시스템 폰트 스택 (Pretendard 선택적 self-host) | 네트워크 의존 최소화 |

### 6.2 그래프 렌더링: 순수 SVG로 확정 (D3 미사용)

D3의 가치는 대부분 **데이터가 유동적일 때** 나온다 — 노드 수가 변하는 그래프의 enter/update/exit 처리, 힘 기반 자동 배치, 대규모 데이터 바인딩. 이 서비스는 그 어느 것도 필요하지 않다.

| 판단 근거 | 내용 |
|---|---|
| 데이터가 고정 | 노드 11개, 엣지 27개. 앱 수명 동안 변하지 않는다 |
| 배치가 고정 | F1.1에서 **원본 다이어그램 좌표를 그대로 쓰기로** 했으므로 force simulation이 아예 불필요하다. D3의 대표 기능을 안 쓴다 |
| 상호작용 범위 | 클릭·호버·줌·팬뿐. `zoom`은 SVG `transform` 속성 갱신 40여 줄로 충분하다 |
| 용량 | d3 전체 ~270KB. 필요한 모듈만 골라도 ~60KB. 총 예산 500KB에서 무시할 수 없는 비중 |
| 빌드 부담 | 모듈 단위로 고르려면 번들러가 필요해진다. **빌드 스텝 없음** 원칙(§6.1)과 충돌 |
| 유지보수 | 이 사이트는 한 번 만들고 수년간 방치될 가능성이 높다. 의존성이 0이면 몇 년 뒤에도 그대로 열린다 |

**구현 방침:** `<svg>` 안에 엣지는 `<path>`(직선/2차 베지어), 화살촉은 `<marker>`, 노드는 `<g><rect>+<text></g>`. 줌·팬은 최상위 `<g>` 하나에 `transform="translate(tx,ty) scale(k)"` 를 적용한다. 엣지 라벨은 `<foreignObject>` 안에 KaTeX 출력을 넣어 수식으로 렌더링한다.

> 예외 조항: F1.8 경로 찾기는 그래프 알고리즘이지 렌더링이 아니다. BFS 20여 줄로 직접 구현한다 (`js/graph/pathfind.js`).

### 6.3 KaTeX: 무엇이고, 왜 self-host인가

**KaTeX가 무엇인가.** 이 사이트는 `f(x) = \frac{\lambda^{r}}{\Gamma(r)} x^{r-1} e^{-\lambda x}` 같은 LaTeX 수식을 화면에 **제대로 조판된 수식**으로 그려야 한다. 브라우저는 LaTeX을 모르므로 이를 번역해 줄 라이브러리가 필요하다. KaTeX는 Khan Academy가 만든 그 번역기로, LaTeX 문자열을 받아 HTML+CSS로 조판된 수식을 만들어 낸다.

대안인 **MathJax**와 비교하면:

| | KaTeX | MathJax |
|---|---|---|
| 렌더링 방식 | **동기적** — 함수를 호출하면 즉시 결과가 나온다 | 비동기 — 렌더링 완료를 기다려야 한다 |
| 속도 | 수십 배 빠름 | 느림 |
| 용량 | ~280KB (JS+CSS+폰트) | ~1MB 이상 |
| LaTeX 문법 지원 | 흔히 쓰는 수학 문법 대부분 | 더 광범위 (매크로, 패키지 등) |

이 사이트는 슬라이더를 움직일 때마다 패널의 수식을 다시 그려야 하고, 필요한 문법은 분수·적분·그리스문자·아래위첨자 정도로 평범하다. **동기적이고 빠른 KaTeX가 명확히 맞다.**

**self-host vs CDN이 무엇인가.** 라이브러리 파일을 어디서 불러올지의 문제다.

- **CDN 방식:** `<script src="https://cdn.jsdelivr.net/npm/katex/...">` 처럼 외부 서버를 가리킨다. 리포지토리에 파일을 넣지 않아도 되고 한 줄이면 끝난다. 대신 **사용자가 페이지를 열 때마다 그 외부 서버에 접속이 되어야** 한다.
- **self-host (vendored) 방식:** KaTeX 파일들을 내려받아 리포지토리의 `vendor/katex/` 에 직접 넣고 `<script src="vendor/katex/katex.min.js">` 로 가리킨다. 리포지토리 용량이 늘고 버전 업데이트를 수동으로 해야 한다. 대신 **외부 의존이 0이 된다.**

**self-host로 확정하는 이유:**

1. **오프라인 요구사항과 직결.** §9에 "최초 로드 후 네트워크 없이 전 기능 동작"을 명시했다. CDN을 쓰면 이 조건을 만족시킬 수 없다 — 수식이 통째로 안 나오면 사실상 사이트가 죽는다.
2. **강의실 환경.** 대학 강의실·기관 네트워크는 외부 CDN을 차단하거나 느리게 하는 경우가 드물지 않다. 교수자가 수업 중에 띄웠는데 수식만 안 나오는 상황은 치명적이다.
3. **장기 보존.** CDN URL은 몇 년 뒤 정책 변경·버전 삭제로 깨질 수 있다. 이 사이트는 오래 방치될 것을 전제한다.
4. **비용이 낮다.** 필요한 파일은 `katex.min.js`, `katex.min.css`, `fonts/` 뿐이고, 폰트는 실제 쓰는 서브셋(KaTeX_Main, KaTeX_Math, KaTeX_Size1–2, KaTeX_AMS)만 남기면 **~150KB 수준**으로 줄어든다. 500KB 예산 안에 들어온다.

**설치 절차 (구현 시 그대로 수행):**
```
1. https://github.com/KaTeX/KaTeX/releases 에서 최신 안정판 dist 다운로드
2. katex.min.js, katex.min.css, fonts/ 를 vendor/katex/ 에 복사
3. fonts/ 에서 woff2 외 형식(ttf, woff) 삭제 — 대상 브라우저는 모두 woff2 지원
4. 실제 렌더링에 쓰이지 않는 폰트 파일 제거 (test/font-audit.html 로 확인)
5. vendor/katex/VERSION.txt 에 버전과 다운로드 날짜 기록
```

**래퍼 규칙 (`js/util/latex.js`):** KaTeX를 코드 곳곳에서 직접 호출하지 않고 `renderTex(latexString, element, {display})` 한 함수로 감싼다. 파싱 실패 시 예외를 던지지 말고 원본 LaTeX 문자열을 `<code>`로 표시하고 콘솔에 경고를 남긴다 — 수식 하나가 깨져도 페이지 전체가 멈추면 안 된다.

### 6.4 파일 구조

리포지토리: **`distribution`** → 배포 주소 `https://databetter25.github.io/distribution/`

```
distribution/
├── index.html                  # 관계도 메인
├── clt.html                    # 중심극한정리 시뮬레이터 (F4.7)
├── about.html                  # 사용법·출처·표기 규약
├── css/
│   ├── base.css                # 리셋, 토큰(색·간격·타이포)
│   ├── graph.css
│   └── panel.css
├── js/
│   ├── main.js                 # 앱 진입점, 라우팅(해시)
│   ├── graph/
│   │   ├── render.js           # SVG 노드·엣지 그리기
│   │   ├── interaction.js      # 클릭·호버·줌·팬
│   │   └── pathfind.js         # F1.8 경로 찾기 (BFS)
│   ├── panel/
│   │   ├── distribution.js     # F2
│   │   ├── relation.js         # F3
│   │   ├── simulation.js       # F4.1–F4.6
│   │   └── clt.js              # F4.7 중심극한정리 시뮬레이터 (독립 화면)
│   ├── plot/
│   │   ├── curve.js            # PDF/CDF 곡선
│   │   ├── histogram.js
│   │   └── axis.js
│   ├── distributions/
│   │   ├── special.js          # gamma, lgamma, beta, erf, incomplete gamma/beta
│   │   ├── pdf.js              # 분포별 밀도함수
│   │   ├── cdf.js
│   │   └── sample.js           # 난수 생성기 + seedable PRNG
│   └── util/
│       ├── latex.js            # KaTeX 래퍼
│       └── validate.js         # §5.3
├── data/
│   ├── distributions.json
│   ├── relations.json
│   └── tours.json              # F6
├── test/
│   ├── numeric.html            # §7.4 CDF 정확도 검증 리포트
│   ├── reference.json          # R로 사전 계산한 참조값
│   └── font-audit.html         # §6.3 KaTeX 폰트 사용 감사
├── vendor/
│   └── katex/                  # katex.min.js, katex.min.css, fonts/(woff2), VERSION.txt
├── README.md
└── .nojekyll                   # GitHub Pages가 vendor/ 등을 그대로 서빙하도록
```

---

## 7. 수치 계산 요구사항

교육용 도구이므로 **정확도보다 견고성(robustness)** 이 우선이다. 극단 모수에서 `NaN`이나 `Infinity`가 화면에 나오는 것이 가장 나쁜 결과다.

### 7.1 구현 필요 특수함수
| 함수 | 용도 | 권장 알고리즘 |
|---|---|---|
| `lgamma(x)` | 모든 감마 계열 PDF | Lanczos 근사 (g=7, 9항) |
| `lbeta(a,b)` | Beta, F | `lgamma` 조합 |
| `erf(x)` | 정규 CDF | Abramowitz–Stegun 7.1.26 또는 유리함수 근사 |
| `lowerIncompleteGamma(s,x)` | Gamma·χ² CDF | 급수(x < s+1) / 연분수(그 외) 전환 |
| `incompleteBeta(a,b,x)` | Beta·t·F CDF | Lentz 연분수 + 대칭 변환 |

### 7.2 수치 안정성 규칙
- **모든 PDF는 로그 스케일에서 계산 후 `exp`한다.** 큰 자유도(예: χ²(500))에서 감마함수 오버플로 방지.
- 지지집합 밖의 x에 대해서는 예외 없이 `0`을 반환한다 (`NaN` 금지).
- 계산 결과가 유한하지 않으면 해당 점을 플롯에서 건너뛰고, 곡선을 끊어 그린다.
- 슬라이더 최대값은 수치적으로 안정한 범위로 제한한다 (예: 자유도 최대 500).

### 7.3 난수 생성
- 기반 PRNG: **mulberry32** 또는 **sfc32** (seedable, 32bit, 빠름)
- 정규: Box–Muller 또는 Marsaglia polar
- 감마: Marsaglia–Tsang (r ≥ 1) + boost 기법 (r < 1)
- 베타: Gamma 두 개의 비 (E11 관계 자체를 재사용 — 교육적으로도 일관)
- 지수/균등: 역변환 (E13 관계 재사용)
- χ², t, F: 정규·감마 조합으로 정의대로 생성
- **F4.7 전용 원천분포 3종**: 양봉 혼합(베르누이 선택 + 정규), 이산 균등(주사위, `floor(6U)+1`), 극단 왜도(Gamma r=0.5 — Marsaglia–Tsang boost 경로 검증용)
- t(1)(코시) 생성 시 극단값이 나오므로, 히스토그램 축은 표본 분위수(1–99%) 기준으로 잡는다 (F4.7.6)

> **설계 원칙:** 표본 생성기를 가능한 한 **사이트가 가르치는 관계 그대로** 구현한다. 코드가 교재 내용과 일치하면, 코드 자체가 부가 학습 자료가 된다.

### 7.4 정확도 수용 기준
- 각 CDF는 참조값(R의 `pgamma`, `pbeta`, `pt`, `pf` 등으로 사전 계산한 테이블) 대비 상대오차 < 1e-6.
- 참조 테이블은 `test/reference.json`으로 저장하고, `test/numeric.html`에서 브라우저 상에서 검증 결과를 표로 출력한다.

---

## 8. UI/UX 요구사항

### 8.1 레이아웃
- **데스크톱 (≥1024px):** 좌측 관계도 캔버스(약 62%) + 우측 상세 패널(약 38%). 패널은 접기 가능.
- **태블릿 (768–1023px):** 관계도 상단, 패널 하단 스택.
- **모바일 (<768px):** 관계도는 스크롤·핀치 확대 가능한 전체 화면. 노드 탭 시 패널이 바텀시트로 올라온다. 25개 엣지 라벨은 모바일에서 기본 숨김, 확대 시 표시.

### 8.2 시각 언어
- 노드는 분포 계열별로 색상군을 구분한다. 예: 정규 계열(파랑), 감마 계열(주황), 균등·베타 계열(초록), 표본분포 계열(보라). 색상만으로 정보를 전달하지 않고 **라벨과 형태를 병행**한다 (§8.4).
- 원본 다이어그램의 차분한 인상(흰 배경 + 파란 화살표)을 존중하되, 상호작용 상태(호버·선택·경로 강조)를 위한 위계를 추가한다.
- 다크 모드를 지원한다 (`prefers-color-scheme` + 수동 토글). 플롯 배경·격자·곡선 색이 모두 대응해야 한다.

### 8.3 언어
- UI 및 설명은 **한국어**를 기본으로 한다.
- 분포명은 `한글명(영문 표기)` 형태로 병기한다 (예: `감마분포 (Gamma)`) — 원서 교재와 대조할 수 있어야 한다.
- 수식 표기는 국제 표준 기호를 그대로 사용한다.

### 8.4 접근성
- 모든 상호작용 요소는 키보드로 도달 가능하다. 관계도는 Tab으로 노드 순회, Enter로 선택, 방향키로 인접 노드 이동.
- 각 노드·엣지에 `aria-label` (예: "감마분포 노드, 연결된 관계 5개").
- 색상 대비 WCAG AA (텍스트 4.5:1) 충족. 엣지 유형은 색이 아닌 **선 스타일**로 구분한다.
- 플롯은 `<canvas>`이므로, 대응하는 데이터 요약을 시각적으로 숨긴 텍스트로 제공한다 (평균, 분산, 최빈값, 모양 서술).

---

## 9. 비기능 요구사항

| 항목 | 요구사항 |
|---|---|
| 성능 | 첫 화면 렌더 < 1.5s (3G Fast 기준). 슬라이더 조작 → 곡선 갱신 < 100ms. 10,000 표본 시뮬레이션 < 300ms |
| 총 용량 | ~~KaTeX 포함 500KB 이하 (gzip 전)~~ → **실제 전송 250KB 이하로 개정 (2026-09-05)**<br>측정: 원본 740KB / 실제 전송 221KB (v0.4, 이산분포 통합 후). 이 중 `katex.min.js` 단독 269KB로 절반을 차지하며 줄일 수 없다. 원래 기준은 KaTeX 크기를 과소평가한 값이었다. GitHub Pages가 텍스트를 gzip하므로 사용자가 실제로 받는 양은 221KB다. 폰트는 실사용 4종만 남겨 168KB→56KB로 줄였다 (`test/font-audit.html`이 회귀를 감시) |
| 호스팅 | 정적 파일만. GitHub Pages, 리포지토리 `distribution` → `databetter25.github.io/distribution/`. 서버·빌드 파이프라인 없음 |
| 오프라인 | 최초 로드 후 네트워크 없이 전 기능 동작 (모든 자원 self-hosted) |
| 브라우저 | Chrome / Edge / Safari / Firefox 최신 2개 버전. IE 미지원 |
| 상태 | 로그인·서버 저장 없음. 상태는 URL 해시로만 표현 |
| 라이선스 | 코드 MIT, 콘텐츠 CC BY 4.0 권장 |

---

## 10. 범위 외 (Out of Scope)

명시적으로 이번 버전에 포함하지 않는다.

- 이산분포 (이항, 포아송, 기하, 음이항, 초기하) 및 그 관계 → **Phase 3 후보**
  - 단, F4.7 CLT 시뮬레이터의 원천분포 옵션 `이산 균등(주사위)` 1종만 예외로 허용한다. "CLT는 연속분포에만 해당한다"는 오개념 교정에 필요하며, 관계도에는 노드로 추가하지 않는다
- 다변량 분포 (다변량 정규, Wishart, Dirichlet)
- 형식적 적합도 검정(KS, Anderson–Darling)의 p값 계산 — F4.4의 요약 통계로 대체
- 사용자 계정, 진도 저장, 퀴즈 채점
- 사용자가 직접 데이터를 업로드해 적합시키는 기능
- 수식 자동 유도(CAS) — 모든 유도는 사전 작성된 데이터

---

## 11. 원본 다이어그램 검토 사항 (구현 전 확정 필요)

첨부 자료를 데이터로 옮기는 과정에서 발견한 표기 문제. **콘텐츠 정확성에 직결되므로 구현 착수 전에 결정한다.**

### 11.1 표준화 변환의 분모 — 표기 오류로 판단
원본은 `(X − μ) / σ²` 로 표기되어 있으나, 표준화의 올바른 변환은 `(X − μ) / σ` 이다. 분모가 σ²이면 결과의 분산이 1/σ²가 되어 N(0,1)이 되지 않는다.
→ **권고: `(X − μ)/σ` 로 수정하여 구현한다.**

### 11.2 감마분포 모수화 — **rate로 확정 (결정 완료)**
원본 다이어그램 안에서 λ가 두 가지 의미로 쓰이고 있었다.

| 위치 | 표기 | 함의 |
|---|---|---|
| Gamma → N (E23) | `μ = rλ, σ² = rλ²` | λ는 **척도(scale)** 모수 |
| Gamma → χ² (E8) | `λ = 1/2` | λ는 **비율(rate)** 모수 (χ²(m) = Gamma(m/2, rate 1/2)) |
| U(0,1) → Exp (E13) | `−λ⁻¹ ln(X)` | λ는 **비율(rate)** — 평균 1/λ |
| Exp → U(0,1) (E14) | `exp(−λX)` | λ는 **비율(rate)** |

즉 E23만 scale 해석이고 나머지는 rate 해석이었다.

**첨부된 감마 밀도함수 그래프(r = 1, 4, 8)로 검증한 결과 rate 모수화가 확정되었다.** 그래프에서 읽히는 세 값이 모두 rate λ = 2와 일치한다:

| 확인 항목 | 그래프 관측값 | rate λ=2 이론값 | 일치 |
|---|---|---|---|
| r=1 의 f(0) | 2.0 | λ = 2.0 | ✓ |
| r=4 의 최빈값 / 높이 | ≈1.5 / ≈0.45 | (r−1)/λ = 1.5 / 0.4481 | ✓ |
| r=8 의 최빈값 / 높이 | ≈3.5 / ≈0.30 | (r−1)/λ = 3.5 / 0.2980 | ✓ |

**확정 사양:**

```
f(x) = λ^r / Γ(r) · x^(r−1) · e^(−λx),   x > 0,  r > 0, λ > 0
평균 = r/λ,   분산 = r/λ²,   MGF = (1 − t/λ)^(−r), t < λ
```

이에 따른 수정 사항:
- **E23 라벨을 `μ = r/λ, σ² = r/λ², r → ∞` 로 수정한다** (원본 `μ = rλ, σ² = rλ²`는 scale 표기)
- E7 `r = 1` → Exp(λ), 평균 1/λ — E13·E14와 정합
- E8 `r = m/2, λ = 1/2` 유지 (§11.3 반영)
- 감마 슬라이더 기본값은 **r = 2, λ = 2** 로 하고, 첨부 그래프를 재현하는 프리셋을 제공한다 (F2.9)

→ 채택한 모수화를 `about.html`의 **표기 규약** 절에 명시하고, 각 분포 패널에도 `rate 모수화` 배지를 표시한다. 교재마다 scale/rate가 갈리므로, 배지 클릭 시 "scale 표기 교재와의 대조표"(θ = 1/λ)를 툴팁으로 띄운다.

### 11.3 χ²(m) ← Gamma 엣지의 자유도
원본 라벨은 `r = 1/2, λ = 1/2` 로, 이는 χ²(1)에만 해당한다. 일반형은 `r = m/2` 이다.
→ **권고: `r = m/2, λ = 1/2` 로 일반화한다.**

### 11.4 F 분포로 가는 엣지의 출발 노드
`(X₁/r₁)/(X₂/r₂)` 엣지는 두 개의 독립 χ² 변수를 필요로 하는 **이항 연산**이다. E11(Gamma → Beta)과 E20이 여기 해당한다.
→ **권고: 이항 관계 엣지는 시각적으로 구분한다** (예: 화살표 시작점에 두 갈래 표시). 상세 패널에서 "입력 확률변수 2개 필요, 독립 가정"을 명시한다.

### 11.5 min(xᵢ) 엣지의 결과 모수
원본은 Exp에서 Exp로 향하는 자기 순환으로만 표기되어 있으나, 결과는 Exp(λ)가 아니라 **Exp(nλ)** 이다.
→ **권고: 라벨을 `min(x₁,…,xₙ) ~ Exp(nλ)` 로 명시한다.** 자기 순환 엣지(E3, E4, E10, E19)는 모두 결과 모수를 라벨에 포함시킨다.

### 11.6 누락된 핵심 관계
§3.3 참조. 특히 `Z² ~ χ²(1)` 은 카이제곱분포의 정의에 해당하며, 이것이 없으면 학생이 정규분포 계열에서 표본분포 계열로 넘어가는 다리를 잃는다.
→ **권고: 최소한 `N(0,1) → χ²(1)` 과 `t(n)` 의 정의 관계 2개는 Phase 1에 포함한다.**

---

## 12. 개발 단계

### Phase 1 — 탐색 가능한 지도 (MVP)
- 데이터 파일 작성: 11개 노드 + **27개 엣지** (원본 25개에 §11 수정사항 반영 + 확장 E26·E27)
- F1.1–F1.6 관계도 렌더링과 기본 상호작용
- F2 전체 (모수 슬라이더 + PDF/CDF + 곡선 고정)
- §7 수치 라이브러리 + `test/numeric.html` 검증 통과
- 각 엣지의 `summaryKo` 한 줄 요약 (유도는 아직 없음)
- **F4.7 중심극한정리 시뮬레이터 전체** — F4.7.3~F4.7.6의 교육 장치 4종 포함
  - 부수 작업: `plot/histogram.js` 와 `distributions/sample.js` 를 Phase 1로 앞당긴다 (원래 Phase 2 예정)
- **완료 기준:** 모든 노드의 PDF가 정상 렌더링되고, 모든 엣지를 클릭해 요약을 볼 수 있으며, CLT 시뮬레이터가 독립 화면으로 동작한다

### Phase 2 — 이해를 돕는 층
- F3 단계별 유도 (27개 엣지 전체 콘텐츠 작성 — **가장 시간이 많이 드는 작업**)
- F4.1–F4.6 엣지 연동 시뮬레이션 데모 5종 (F4.7의 히스토그램·표본생성 기반 재사용)
- F1.7 URL 라우팅, F1.8 경로 찾기
- 다크 모드, 반응형 모바일 레이아웃
- **완료 기준:** 임의의 엣지에서 유도 또는 수렴 시연 중 하나 이상을 제공한다

### Phase 3 — 확장
- F5 검색·필터, F6 가이드 투어
- §3.3 확장 관계 전체 추가
- 이산분포 계열 확장 검토
- 접근성 감사 및 키보드 내비게이션 완성

---

## 13. 수용 기준 (Acceptance Criteria)

Phase 1 릴리스 판정 기준:

- [ ] `test/numeric.html` 의 모든 CDF 검증 항목이 상대오차 1e-6 이내로 통과한다
- [ ] `js/util/validate.js` 의 데이터 정합성 검사가 오류 0건이다
- [ ] 11개 분포 각각에 대해, 모수 슬라이더를 최소·최대 끝까지 이동시켜도 플롯에 `NaN`·빈 화면·콘솔 오류가 발생하지 않는다
- [ ] 27개 엣지 전부가 클릭 가능하며 요약 문구를 표시한다
- [ ] 감마 패널에서 `교재 그림` 프리셋(λ=2, r=1·4·8)을 누르면 첨부 원본 그래프와 동일한 형태가 재현된다 (r=1의 f(0)=2.0, r=4 최빈값 1.5, r=8 최빈값 3.5)
- [ ] 네트워크 차단 상태에서 모든 LaTeX 수식이 정상 렌더링된다 (KaTeX self-host 확인)

**CLT 시뮬레이터 (F4.7) 전용 기준**

- [ ] `n = 1` 에서 중앙 패널의 히스토그램이 좌측 원천분포 PDF와 육안으로 일치한다 (모든 원천분포에 대해)
- [ ] 원천분포 `Exp(λ)`, `n = 30`, `R = 10,000` 에서 표준화된 표본평균의 표본평균이 0 ± 0.05, 표본표준편차가 1 ± 0.05 이내다
- [ ] `n` 고정 후 `R` 을 100 → 50,000 으로 올렸을 때 히스토그램 **모양이 변하지 않는다** (매끄러워지기만 한다) — F4.7.3 검증
- [ ] 원천분포 `t(1)` 선택 시 경고 배너가 표시되고, `n` 을 1 → 100 으로 올려도 히스토그램 폭이 좁아지지 않는다 — F4.7.6 검증
- [ ] `R = 50,000`, `n = 100` (표본 500만 개) 조건에서 UI가 정지하지 않고 진행률이 표시되며, 10초 내 완료된다
- [ ] 표준화 토글을 끄면 `n` 증가에 따라 `X̄` 분포가 μ 주위로 좁아진다 (LLN) — F4.7.4 검증
- [ ] 브라우저 네트워크를 차단한 상태에서 새로고침해도 전 기능이 동작한다 (오프라인)
- [ ] 1920×1080 및 390×844(모바일) 뷰포트에서 레이아웃이 깨지지 않는다
- [ ] `about.html` 에 채택한 모수화 규약(§11.2)과 원본 대비 수정 사항(§11.1, 11.3, 11.5)이 명시되어 있다
- [ ] 첫 화면 렌더 1.5초 이내 — **측정 124ms (PASS)**
- [ ] 슬라이더 조작 → 곡선 갱신 100ms 이내 — **측정 중앙값 33ms (PASS)**
- [ ] 실제 전송 용량 250KB 이하 — **측정 221KB (PASS, §9 개정 기준)**
- [ ] `test/font-audit.html` 이 수식 264개를 모두 파싱하고 누락 폰트가 없는지 — **PASS**
- [ ] n 스윕 애니메이션에서 R 이 전 구간 고정이고 n 이 단조 증가하며 왜도가 줄어드는지 (test/clt.html C7) — **PASS**

---

## 부록 A. 결정 기록 (Decision Log)

착수 전 결정 항목은 모두 확정되었다. **미결 항목 없음 — 구현 착수 가능.**

| # | 항목 | 결정 | 근거 | 반영 위치 |
|---|---|---|---|---|
| D1 | 감마 모수화 | **rate** — `f(x) = λ^r/Γ(r)·x^(r−1)e^(−λx)`, 평균 `r/λ` | 첨부 감마 그래프(r=1,4,8)를 수치 검증한 결과 rate λ=2와 정확히 일치. 원본 다이어그램 4개 엣지 중 3개도 rate | §11.2, §3.1, §5.1 |
| D2 | 확장 엣지 Phase 1 포함 | **E26 `Z² ~ χ²(1)`, E27 t분포 정의 2개만 포함** (총 27개 엣지). 나머지 4개는 Phase 3 | E26·E27은 없으면 다른 관계를 이해할 수 없는 구조적 링크(t는 원본에서 들어오는 엣지가 0인 고아 노드). 대칭·역수 관계 4개는 몰라도 지장 없음 | §3.3 |
| D3 | 그래프 라이브러리 | **순수 SVG, D3 미사용** | 노드·엣지 고정, 배치도 고정 좌표라 D3의 핵심 기능(force layout, enter/update/exit)을 아예 안 씀. 의존성 0 = 빌드 스텝 0 = 수년 뒤에도 그대로 동작 | §6.1, §6.2 |
| D4 | KaTeX 호스팅 | **self-host (`vendor/katex/`)**, woff2 서브셋만 유지 (~150KB) | 오프라인 동작이 요구사항이고, 강의실·기관 네트워크의 CDN 차단 위험이 실재. 수식이 안 나오면 사이트가 죽는 것과 같음 | §6.1, §6.3 |
| D5 | 리포지토리·배포 경로 | **`distribution`** → `https://databetter25.github.io/distribution/` | 사용자 지정 | §6.4, §9 |
| D6 | 중심극한정리 | **관계도 엣지가 아닌 독립 시뮬레이터(F4.7)로 구현, Phase 1 포함** | CLT는 모든 노드에 적용되는 메타 관계라 엣지로 그리면 화살표 11개가 생겨 그래프가 무너진다. 또 "변환"이 아니라 새 통계량의 극한이라 E1–E27과 논리적 층위가 다르다. 대신 모든 분포 패널과 E22·E23에서 진입 링크를 건다 | §3.4, F4.7 |

| D7 | 이산분포 통합 (2026-09-05) | **전체 관계도(index) 하나에 연속·이산을 모두 싣고, 이산 관계도(discrete)는 같은 데이터의 부분집합 뷰로 둔다** | 참조 다이어그램을 다시 대조한 결과 이산 6종과 연속 3종(코시·와이블·이중지수)이 빠져 있었다. 데이터 파일을 나누면 두 화면의 설명이 갈라지므로 `graphs` 필드로 구분하는 단일 데이터셋을 택했다 | §3.1, §5.1, `js/util/graphdata.js` |
| D8 | 기하·음이항 규약 (2026-09-05) | **시행 횟수** — `Geom(p)` 지지집합 k=1,2,…, `NB(r,p)` 지지집합 k=r,r+1,… | 이 규약에서만 NB = Geom의 합, r=1 → Geom, `pX → Exp(1)` 이 상수 보정 없이 성립한다. 참조 다이어그램의 `λ=n(1−p)` 는 실패 횟수 규약이므로 `X−r` 로 옮겨 표기했다 | about.html, D5·D6·D20 |
| D9 | 와이블 모수화 (2026-09-05) | **rate 계열** — `F(x) = 1 − e^(−a x^b)`, `b=1` 이면 `Exp(a)` | 참조 다이어그램의 `Weibull(a,b)` 와 `b=1 → 지수` 를 그대로 만족시키는 유일한 해석이며, 사이트 전체의 rate 규약과도 일관된다 | about.html, E36·E37 |

### v0.4 에서 추가된 것 (2026-09-05)

| 구분 | 내용 |
|---|---|
| 노드 | 이산 6종(베르누이·이항·초기하·기하·음이항·포아송) + 이산균등 1종, 연속 3종(코시·와이블·이중지수) — 총 21종 |
| 엣지 | 연속 8개(E32–E39: 코시 계열 4, 와이블 2, 이중지수 2), 이산 22개(D1–D22) — 총 61개 |
| 시뮬레이션 | 27종(연속 21 + 이산 6). 새 레시피 5종은 KS 검정 통과 |
| CLT | 관계도의 21종 전부를 원천분포로 지원. 코시는 평균·분산이 없어 반례로 동작 |
| 학습 경로 | 4종 — `이산에서 연속으로` 추가 |
| 검증 | `test/discrete.html` 추가, `test/numeric.html` 에 새 연속 3종 케이스 추가, 참조표 42블록으로 확장 |

### 이후 남은 판단 (구현 중 결정해도 무방)
- 노드 색상군 구체 팔레트 (§8.2) — 다크 모드 대비까지 함께 확인
- F6 가이드 투어의 경로 구성 (§4 F6.2 예시 4종은 초안)
